import SwiftUI

extension Color {
    static let primaryText = Color("Theme/PrimaryText")
    static let secondaryText = Color("Theme/SecondaryText")
    static let primaryAction = Color("Theme/PrimaryAction")
    static let accentHighlight = Color("Theme/AccentHighlight")
    static let subtleBackground = Color("Theme/Surface")
}